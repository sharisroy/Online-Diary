<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fishermen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.onlinediary.AccountActivity.PreviousNotesActivity"></activity>
        <activity android:name="com.example.onlinediary.AccountActivity.NotesActivity" />
        <activity android:name="com.example.onlinediary.AccountActivity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.onlinediary.MainActivity" />
        <activity android:name="com.example.onlinediary.AccountActivity.SignUpActivity" />
    </application>

</manifest>